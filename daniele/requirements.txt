# Task 2.3.3 - dipendenze.
#
# TUTTE GIA' PRESENTI in ~/pyvenv su ogni macchina del cluster (verificato su scheduler
# e worker): prima di lanciare non c'e' niente da installare. Questo file documenta le
# versioni con cui il task e' stato scritto, e serve a ricostruire l'ambiente altrove:
#
#     python3 -m pip install -r daniele/requirements.txt
#
# python sulle VM: 3.10.12

dask[complete]==2026.6.0
distributed==2026.6.0
pandas==2.3.3
numpy==2.2.6
pyarrow==24.0.0
matplotlib==3.10.9
bokeh==3.9.1        # serve a performance_report: senza, la misura fallisce a fine run
asyncssh==2.24.0    # SSHCluster (avvio del cluster multi-nodo)
notebook==7.6.0
